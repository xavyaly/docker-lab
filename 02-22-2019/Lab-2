// As of now we have been played with images present in docker hub

// DOCKERFILE

Example:

FROM ubuntu

ADD . /app

RUN apt-get update
RUN apt-get upgrade -y
RUN apt-get install -y nodejs ssh mysql
RUN cd /app && npm install

# This should start three process, mysql, ssh in the backgorund 
# And node app in foreground

CMD mysql & sshd & npm start

Execution:

$ docker build -t <tag-name> <path-of-dockerfile>

Example to create own image:

$ docker build latest:npm Dockerfile .
$ docker build -t wtf .


// Read about the docker compose
https://docs.docker.com/compose/


// Read about the LIVE Scripts:
https://github.com/docker-library/golang/blob/ad773d11d0bdf21d1f4bc4adf7ea580e71f49d10/1.12-rc/stretch/Dockerfile
